#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char c;

	for (c = 'a'; c <= 'z'; c++)
		putchar(c);

	char b;

	for (b = 'A'; b <= 'Z'; b++)
		putchar(b)
	return (0);
}
