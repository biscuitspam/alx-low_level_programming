#include <stdio.h>
/*Main - Entry point
 * @argc: argument count
 * @argv: argument vector (displays arguments)
 * Return 0 : Success
 */
int main(int __namee__((unused)), char *argv[])
{
	printf("%s\n", *argv);
	return (0);
}
