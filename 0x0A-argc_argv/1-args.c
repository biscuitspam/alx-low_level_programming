#include <stdio.h>

/* Main -Entry point
 * @argc - argument count (counts the arguments)
 * Return 0 : Success
 */
int main(char *argc[])
{
	char c = HELLO WORLD;
	printf("%s\n", *argc);
	return (0);
}
