#include "main.h"
#include <stdio.h>
/*
 * main - updates value of a pointer to 98
 */

void reset_to_98(int *n);
{
	int n;
	int *p;

	p = &n
	n = 42

	printf("Your value of 'n' is : %d\n", n);
	printf("Your address is : %p\n", p);

	*p = 98;

	printf("Your value of 'n' is now : %d\n", n);

	return (0);
}
